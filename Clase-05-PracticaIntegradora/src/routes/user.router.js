import { Router } from 'express';

const router = Router();

//Registro de usuario
router.post('/register', (req,res) => {
    //Obtener los datos del body
    //Crear un usuario a partir de los datos
    
})

//Login 
router.post('/login', (req,res) => {
    
    //Obtener los datos del body

    //Tenemos que validar la contraseña

    //Obtenemos los datos del usuario y lo guardamos en un JWT

    //Lo guardamos en una cookie para el usuario

})

export default router;